<%= form_with(model: Post, local: true) do |f| %>

  <%= f.label :Create_Post %>
  <%= f.text_field :content, placeholder: "What's on your mind?", class: 'post-form' %>

  <%= f.submit "Post!", class: "btn btn-primary" %>
<% end %>