<header style="border: 1px solid;">
  <span class="fb">FACEBOOK CLONE</span>
  <div class="top-panel">
    <% if signed_in? %>
      <span><%= link_to "Log out", destroy_user_session_path, method: :delete %></span>
    <% else %>
      <% if action_name != "new" %>
        <span><%= link_to "Sign up", new_user_registration_path %></span>
      <% end %>
    <% end %>
    <span>Kerron</span>
    <span>Settings</span>
    <span>Log out</span>
  </div>
</header>