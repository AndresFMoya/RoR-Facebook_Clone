<%= form_with(model: Comment.create) do |f| %>

  <%= f.label :content %>
  <%= f.text_field :content, class: 'form-control' %>

  <%= hidden_field_tag :user_id, current_user.id %>
  <%= hidden_field_tag :post_id, 1 %>

  <%= f.submit "Save", class: "btn btn-primary" %>
<% end %>